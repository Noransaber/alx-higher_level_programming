#!/usr/bin/python3
""" displays all values in the state """

import MySQLdb
from sys import argv

if __name__ == "__main__":
	conn = MySQLdb.connect(
        host="localhaost",
        port=3306,
        user=argv[1],
        passwd=argv[2],
        db=rgv[3],
        charset="utf8",
    )
    cur = conn.cursor()
